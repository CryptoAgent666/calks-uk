# Cloudflare Pages _redirects
# https://developers.cloudflare.com/pages/configuration/redirects/

# 1. /sitemap.xml → sitemap-index.xml (Google probes default location)
/sitemap.xml /sitemap-index.xml 301

# 2. Trailing slash normalisation handled by Astro `trailingSlash: 'always'`
# 3. Double-slash normalisation — Cloudflare auto-normalises consecutive
#    slashes in URL paths at the edge before this file runs.

# 4. Legacy / common typo redirects
/sitemap/ /sitemap-index.xml 301
/sitemap /sitemap-index.xml 301
/calculator/ /calculators/ 301
/all/ /calculators/ 301
/all-calculators/ /calculators/ 301

# 5. Catch-all for plural-typo: /calculators/<slug>/ → /calculator/<slug>/
# (Google or external links may guess plural form since listing is at /calculators/)
/calculators/:slug/ /calculator/:slug/ 301
/calculators/:slug /calculator/:slug/ 301

# 6. Category typos: /category (singular) → /category/ list page
/category/ /calculators/ 301
/categories/ /calculators/ 301
/categories/:slug/ /category/:slug/ 301

# 7. Pseudo-extensions
/index.html / 301
/calculator/:slug/index.html /calculator/:slug/ 301
/calculator/:slug.html /calculator/:slug/ 301
